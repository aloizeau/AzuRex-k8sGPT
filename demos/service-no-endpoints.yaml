apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: dev
spec:
  selector:
    app.kubernetes.io/name: non-existent-label  # Aucun Pod ne correspond à ce label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
